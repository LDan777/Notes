# 从谱理论到空间架构

## 第一部分：矩阵几何学与子空间夹角（SVD 视角）

在经典代数中，我们通过内积定义两个向量的夹角。然而，当面对**高维空间中的两个子空间**（例如 $\mathbb{R}^5$ 中的一个三维空间与一个五维空间）时，它们之间的几何相对位置无法仅用一个简单的夹角来刻画，而是存在**一组夹角**，我们称之为**主角（Principal Angles）**或**经典角（Canonical Angles）** 。

### 1. 数学定义与构造步骤

设 $\mathcal{U} \subset \mathbb{R}^d$ 是一个 $m$ 维子空间， $\mathcal{V} \subset \mathbb{R}^d$ 是一个 $n$ 维子空间 。不失一般性，不妨设 $m \le n$ 。

1. **选取标准正交基：** 分别取两个子空间的标准正交基，并将其排列为矩阵的列向量：

$$U = [\mathbf{u}_1, \mathbf{u}_2, \dots, \mathbf{u}_m] \in \mathbb{R}^{d \times m}, \quad \text{满足 } U^TU = I_m$$


$$V = [\mathbf{v}_1, \mathbf{v}_2, \dots, \mathbf{v}_n] \in \mathbb{R}^{d \times n}, \quad \text{满足 } V^TV = I_n$$


2. **构造两两内积矩阵：** 构造矩阵 $M = V^T U \in \mathbb{R}^{n \times m}$ 。这个矩阵的第 $(i,j)$ 个元素就是 $\mathbf{v}_i$ 与 $\mathbf{u}_j$ 的内积，相当于把所有可能的基向量组合的内积全算了一遍 。


3. **奇异值分解（SVD）：** 对矩阵 $M$ 进行奇异值分解 。所得的每一个奇异值 $\sigma_i$ 恰好对应了子空间夹角的余弦值：



$$\sigma_i(V^T U) = \cos\theta_i \quad (i = 1, 2, \dots, m)$$



### 2. 核心合理解析（老师强调的检验点）

* **奇异值的非负与有界性：** 因为 $U$ 和 $V$ 的列向量都是单位正交向量，根据柯西-施瓦茨不等式，其内积绝对值必不大于 1 。因此其奇异值满足 $0 \le \sigma_i \le 1$ 。这保证了 $\cos\theta_i \in [0, 1]$ ，从而能在 $[0, \pi/2]$（第一象限）内唯一确定一个锐角 $\theta_i$ 。


* **基的选取无关性（合理性证明）：** 空间里的基有无数种选法，这个定义必须跟基的选取无关，才是空间固有的几何属性 。


* *证明：* 假设换一组标准正交基 $\tilde{U} = U Q_1$ 和 $\tilde{V} = V Q_2$ ，其中 $Q_1, Q_2$ 分别是 $m$ 阶和 $n$ 阶的正交矩阵 。


* 此时新的内积矩阵为： $\tilde{M} = \tilde{V}^T \tilde{U} = (V Q_2)^T (U Q_1) = Q_2^T (V^T U) Q_1$ 。


* 因为 $Q_1, Q_2$ 是正交矩阵，根据奇异值的性质，对一个矩阵两边乘正交矩阵**不改变其奇异值** 。因此算出来的奇异值（即夹角余弦值）完全一致 。




* **排序约定：** 习惯上奇异值按降序排列（ $\sigma_1 \ge \sigma_2 \dots$） 。由于余弦函数在 $[0, \pi/2]$ 上是单调递减的，所以对应的经典角呈**升序排列**（ $\theta_1 \le \theta_2 \dots$），即最大的奇异值对应最小的几何夹角 。



---

### 3. 三个几何实例的代数对齐

#### 实例一：两条直线（ $m=1, n=1$）

* **几何直观：** 两条直线相交会产生两个锐角和两个钝角，人们直觉上默认选那个小的锐角 。


* **代数对齐：** 基向量内积可正可负，但奇异值（作为奇异值分解的结果）一定是非负的（相当于加了绝对值） 。因此通过 $\cos\theta = |\mathbf{v}^T\mathbf{u}|$ 算出来的永远是那个 $\le 90^\circ$ 的锐角，与几何直觉完美不冲突 。



#### 实例二：直线与平面（ $m=2, n=1$）

* **几何直观：** 高中立体几何中，求直线与平面的夹角是将直线投影到平面上，斜线与投影线所成的角就是线面角 。


* **代数对齐：** 平面内可以随便取正交基 。我们聪明地让 $\mathbf{u}_1$ 沿着直线的投影方向，让 $\mathbf{u}_2$ 在平面内垂直于 $\mathbf{u}_1$ 。此时直线向量 $\mathbf{v}$ 与 $\mathbf{u}_2$ 垂直（内积为 0），与 $\mathbf{u}_1$ 的内积恰好是 $\cos\theta$ 。
构造矩阵： $V^T U = \mathbf{v}^T [\mathbf{u}_1, \mathbf{u}_2] = [\cos\theta, 0]$ 。对其求奇异值，只有唯一一个非零奇异值，就是 $\cos\theta$ 。



#### 实例三：三维空间中的两个平面（ $m=2, n=2$ 在 $\mathbb{R}^3$ 中）

* **几何直观：** 三维空间中两个平面相交于一条直线（交线） 。


* **代数对齐：** 先找出交线方向，将其作为两组基的第一个向量（令 $\mathbf{u}_1 = \mathbf{v}_1$ 为交线的单位方向向量） 。因为它们完全重合，所以内积 $\mathbf{u}_1^T \mathbf{v}_1 = 1 = \cos(0^\circ)$ 。
接着，在各自平面内找垂直于交线的方向 $\mathbf{u}_2$ 和 $\mathbf{v}_2$ 。这两个方向的夹角就是我们熟悉的“二面角” $\theta$ 。
最终算出来的两个主角分别是： $\theta_1 = 0$（顺着交线看没有夹角）和 $\theta_2 = \theta$（垂直于交线看是二面角） 。



> 
> 老师的算法视界 ：
> “高维经典角可以用来刻画算法的收敛性 。比如在很多优化算法中，一个子空间慢慢向另一个固定子空间靠拢叠合 。我们只需要证明它们之间**最大的那个经典角趋向于 0**（即最小的奇异值趋向于 1），就能说明一个空间完全落入了另一个空间中 。”
> 
> 

---

## 第二部分：埃尔米特矩阵的谱分析与极值定理

### 1. 瑞利商（Rayleigh Quotient）与数值域

对于一个**埃尔米特矩阵** $A \in \mathbb{C}^{n \times n}$（满足 $A^* = A$ ，若为实矩阵则为对称矩阵 $A^T = A$） ，其特征值全部为实数 。我们将特征值从小到大排序： $\lambda_1 \le \lambda_2 \le \dots \le \lambda_n$ ，对应的正交归一特征向量为 $q_1, q_2, \dots, q_n$ 。

对于任意非零向量 $x \in \mathbb{C}^n$ ，定义**瑞利商**为 ：


$$R_A(x) = \frac{x^*Ax}{x^*x} \quad (x \neq 0)$$

如果我们对 $x$ 进行归一化约束，即限制在单位球面 $\|x\|_2 = 1$（ $x^*x = 1$）上，瑞利商就变成了二次型 $x^*Ax$ 。

#### 约束优化视角下的求导推导（补充板书细节）

老师提到，可以用带等式约束的优化问题来解释瑞利商的极值 。构造拉格朗日函数 ：


$$L(x, \lambda) = x^*Ax - \lambda(x^*x - 1)$$

对实向量 $x$ 求导（或在复数域下对 $x^*$ 求偏导）并令其为 0 ：


$$\frac{\partial L}{\partial x} = 2Ax - 2\lambda x = 0 \implies Ax = \lambda x \quad$$

**精妙结论：** 该约束优化问题的驻点（一阶导数为 0 的点）恰好就是 $A$ 的**特征向量**，而此时的瑞利商函数值 $x^*Ax = x^*(\lambda x) = \lambda$ 恰好就是对应的**特征值** 。

因为单位球面是一个紧集（有界闭集），连续函数在其上必然能取得最大值和最小值 。代入特征向量后立刻得到**瑞利-里茨定理（Rayleigh-Ritz Theorem）** ：


$$\lambda_1 \le R_A(x) \le \lambda_n \quad \implies \quad \lambda_1 = \min_{\|x\|=1} x^*Ax, \quad \lambda_n = \max_{\|x\|=1} x^*Ax \quad $$

整个瑞利商构成的全体值的集合称为矩阵的**数值域（Numerical Range）** 。因为单位球是连通的，所以埃尔米特矩阵的数值域是一个**连续的闭线段 $[\lambda_1, \lambda_n]$** 。

---

### 2. 库朗-费歇尔极大极小定理（Courant-Fischer Minimax Theorem）

如果我们不仅想找两端的 $\lambda_1$ 和 $\lambda_n$ ，还想用类似的方式把中间的任意第 $k$ 个特征值 $\lambda_k$ 挖出来，该怎么办 ？定理给出了空间维数约束下的极值描述 ：

#### 定理公式

$$\lambda_k = \min_{\dim S = k} \max_{\substack{x \in S \\ \|x\|_2=1}} x^*Ax \quad$$

$$\lambda_k = \max_{\dim S = n-k+1} \min_{\substack{x \in S \\ \|x\|_2=1}} x^*Ax$$

#### 深入本质的思维模型（以第一个公式 $\min-\max$ 为例）

这是一个两阶段的博弈游戏 ：

1. **第一阶段（内部 $\max$）：** 先在整个 $n$ 维空间里随意挑一个 $k$ 维子空间 $S$ 。在这个子空间内部，瑞利商能达到的最大值是 $\max x^*Ax$ 。


2. **第二阶段（外部 $\min$）：** 接下来，你拥有改变这个子空间 $S$ 走向和旋转角度的权力 。为了找出保底线，你要尽可能地去压低这个子空间内部的最大值（取 $\min$） 。


3. 当你遍历所有可能的 $k$ 维子空间，把那个局部的最高点压到退无可退、避无可避的时候，这个最终的保底最大值，恰好就是 $\lambda_k$ 。



#### 严谨完整的夹逼法证明（补全课堂未尽推导）

要证定理成立，必须分两步证明：一是“存在某个子空间能达到 $\lambda_k$”，二是“任何其他子空间里的最大值都绝不会比 $\lambda_k$ 小”。

**第一步：证明 $\min-\max \le \lambda_k$**
我们构造一个极其特殊的 $k$ 维子空间 $S_0$ ，它恰好由矩阵 $A$ 的前 $k$ 个特征向量张成 ： $S_0 = \text{span}\{q_1, q_2, \dots, q_k\}$ 。
对于该子空间中的任意单位向量 $x \in S_0$ ，它可以表示为这组标准正交基的线性组合： $x = \sum_{i=1}^k c_i q_i$ ，且满足 $\sum_{i=1}^k |c_i|^2 = 1$ 。
此时计算它的二次型值 ：


$$x^*Ax = \left(\sum_{i=1}^k c_i q_i\right)^* A \left(\sum_{j=1}^k c_j q_j\right) = \sum_{i=1}^k \lambda_i |c_i|^2 \le \lambda_k \sum_{i=1}^k |c_i|^2 = \lambda_k \quad$$

由此可知，在这个特定的子空间 $S_0$ 内部，瑞利商的最大值就是 $\lambda_k$ （当 $x = q_k$ 时取到等号 ）。
既然我们找到了一个特定的子空间让最大值为 $\lambda_k$ ，那么当我们对**所有** $k$ 维子空间取最小值时，其结果绝不会超过 $\lambda_k$ ：


$$\min_{\dim S = k} \max_{x \in S, \|x\|=1} x^*Ax \le \max_{x \in S_0, \|x\|=1} x^*Ax = \lambda_k \quad $$

**第二步：证明 $\min-\max \ge \lambda_k$**
现在，假设有人在空间里任选了一个**完全随机**的 $k$ 维子空间 $S$ 。
我们同时关注另一个特定的特征子空间 $W$ ，它由后 $n-k+1$ 个特征向量张成： $W = \text{span}\{q_k, q_{k+1}, \dots, q_n\}$ 。
根据**维数公式（Dimension Formula）**，这两个子空间在全空间 $\mathbb{R}^n$ 里必定会撞在一起 ：


$$\dim(S \cap W) = \dim S + \dim W - \dim(S + W) \ge k + (n-k+1) - n = 1 \quad$$

这说明，无论你如何旋转、隐藏这个随机的 $k$ 维子空间 $S$ ，它与特征空间 $W$ 之间**必然存在至少一个非零的相交向量 $x_0$** 。
我们对这个交集中的单位向量 $x_0 \in S \cap W$ 进行分析 ：

* 因为 $x_0 \in S$ ，所以在子空间 $S$ 内部取最大值时，必然有： $\max_{x \in S, \|x\|=1} x^*Ax \ge x_0^* A x_0$ 。


* 又因为 $x_0 \in W$ ，它可以表示为后方特征向量的组合： $x_0 = \sum_{i=k}^n d_i q_i$（满足 $\sum |d_i|^2 = 1$）。其二次型满足：


$$x_0^* A x_0 = \sum_{i=k}^n \lambda_i |d_i|^2 \ge \lambda_k \sum_{i=k}^n |d_i|^2 = \lambda_k \quad $$


将两边拼起来：对于**任意**挑选的 $k$ 维子空间 $S$ ，其内部的最大值都无法逃脱拦截，必定大等于 $\lambda_k$（ $\max_{x \in S} x^*Ax \ge \lambda_k$） 。
既然对每一个子空间的最大值都 $\ge \lambda_k$ ，那么对所有子空间取最小值也必然有 ：



$$\min_{\dim S = k} \max_{x \in S, \|x\|=1} x^*Ax \ge \lambda_k \quad $$



**终极大招：** 两边夹逼成功，定理得证 。



---

### 3. 柯西交错定理与特征值扰动

#### 柯西交错定理（Cauchy's Interlacing Theorem）

设 $A$ 是一个 $n$ 阶埃尔米特矩阵 。如果我们对 $A$ 进行“降维截取”，抹去最后一列和最后一行，得到一个 $n-1$ 阶的**主子阵** $A_{n-1}$ 。那么 $A_{n-1}$ 的特征值 $\mu_i$ 会严丝合缝地“交错插空”在原矩阵 $A$ 的特征值 $\lambda_i$ 之间 ：


$$\lambda_1 \le \mu_1 \le \lambda_2 \le \mu_2 \le \dots \le \lambda_{n-1} \le \mu_{n-1} \le \lambda_n \quad$$

> 
> 老师的逻辑延伸 ：
> 如果不仅降一阶，而是连续砍掉好几阶，变成一个 $m$ 维的子矩阵，这个交错性质依然会像多米诺骨牌一样传下去，这就是庞加莱交错定理（Poincaré Interlacing） 。有了库朗-费歇尔定理这个特征值的刻画工具，我们才能真正证明子矩阵特征值与原矩阵的深层定量关系 。
> 
> 

#### 韦尔扰动定理（Weyl's Perturbation Theorem）

在现实计算中，矩阵常伴随误差或扰动（ $A \to A + \Delta A$） 。如果扰动阵 $\Delta A$ 也是埃尔米特矩阵 ，其特征值如何改变？
老师在黑板上给出了清晰的代数推导 ：


$$\lambda_i(A + \Delta A) = \min_{\dim S = i} \max_{x \in S, \|x\|=1} x^*(A + \Delta A)x = \min_{\dim S = i} \max_{x \in S, \|x\|=1} [x^*Ax + x^*(\Delta A)x] \quad $$

由于任何单位向量作用在扰动二次型上，都不可能突破扰动阵自身特征值的边界（即 $\lambda_1(\Delta A) \le x^*(\Delta A)x \le \lambda_n(\Delta A)$） ，直接将其代入放大 ：


$$\lambda_i(A + \Delta A) \le \min_{\dim S = i} \max_{x \in S, \|x\|=1} [x^*Ax + \lambda_n(\Delta A)] = \lambda_i(A) + \lambda_n(\Delta A) \quad $$

同理，利用下界 $\lambda_1(\Delta A)$ 在另一端缩小可得 ： $\lambda_i(A + \Delta A) \ge \lambda_i(A) + \lambda_1(\Delta A)$ 。
最终合成**韦尔双向扰动不等式** ：


$$\lambda_i(A) + \lambda_1(\Delta A) \le \lambda_i(A + \Delta A) \le \lambda_i(A) + \lambda_n(\Delta A) \quad $$

若用矩阵的谱范数（2-范数）来度量（ $\max(|\lambda_1|, |\lambda_n|) = \|\Delta A\|_2$） ，立刻导出更常用的工程结论 ：


$$|\lambda_i(A + \Delta A) - \lambda_i(A)| \le \|\Delta A\|_2 \quad $$

> 
> 老师的敲黑板提醒 ：
> “非埃尔米特矩阵在受到微小扰动时，特征值可能会发生疯狂的跳跃（比如若尔当块导致的开方爆炸） 。而埃尔米特矩阵由于具备韦尔定理的保驾护航，扰动有多大，特征值最多就偏移多少，这在数值算法里叫作‘极佳的稳定性’ 。”
> 
> 

#### 霍夫曼-韦兰德不等式（Hoffman-Wieland Theorem）

韦尔定理刻画的是“单个特征值”的最坏扰动 ，而霍夫曼-韦兰德不等式则用 **Frobenius 范数（F-范数，即矩阵元素平方和开根号）** 整体刻画了“全体特征值”的均方根误差 ：


$$\sqrt{\sum_{i=1}^n |\lambda_i(A+E) - \lambda_i(A)|^2} \le \|E\|_F \quad $$

---

## 第三部分：奇异值低秩逼近与埃卡特-扬定理

### 1. 埃卡特-扬定理（Eckart-Young-Mirsky Theorem）

定理指出：给定一个矩阵 $A$（其奇异值为 $\sigma_1 \ge \sigma_2 \dots$） ，如果你想在全空间中寻找一个**秩不超过 $k$ 的低秩矩阵 $B$** 来近似推进 $A$ ，那么通过 SVD 截断（仅保留前 $k$ 个最大奇异值，其余强行置 0）得到的矩阵 $A_k$ 就是绝对最优解 。

其精确的阶段性截断误差公式如下 ：

* 在 2-范数（谱范数）度量下 ：



$$\min_{\text{rank}(B) \le k} \|A - B\|_2 = \|A - A_k\|_2 = \sigma_{k+1}(A) \quad $$



*几何含义：* 用低秩矩阵逼近时，在空间最坏的方向上，最大的截断误差恰好等于被你扔掉的**第一个（也是剩余里最大的）奇异值 $\sigma_{k+1}$** 。


* 在 Frobenius 范数（F-范数）度量下 ：



$$\min_{\text{rank}(B) \le k} \|A - B\|_F = \|A - A_k\|_F = \sqrt{\sum_{i=k+1}^{\min(m,n)} \sigma_i^2(A)} \quad $$



*几何含义：* 低秩逼近带来的**总能量损失（信息平方和）**，正好等于所有被无情抛弃的尾部奇异值的平方和 。



2. 老师的现场演示：图像低秩近似 

* **背景：** 老师在课堂上用 MATLAB/Python 演示了一张 $256 \times 256$ 的黑白照片 。这在代数上就是一个 $256 \times 256$ 的满秩矩阵 $A$ 。


* **原理：** 现实世界的图像具有极强的**局部连续性** （比如相邻的几列像素长得差不多，高分辨率只是增加了列的冗余度） 。因此，对其做 SVD 分解后，会发现奇异值衰减得非常快，前面几个很大，后面迅速变小 。


* 实验结果 ： * 当老师强行丢弃 $k$ 之后的奇异值，仅保留 **$k = 32$** 的秩时，图片虽然变糊了一点，但人眼依然能清晰认出照片的内容 。这说明尾部的奇异值包含的“能量/信息”微乎其微 。


* 这种低秩近似是现代矩阵压缩、降噪以及推荐系统算法（如矩阵分解）的数学钢筋铁骨 。





---

## 第四部分：仿射几何学（无原点锁链的自由几何）

传统线性代数是一个带有“原点情结”的系统，它要求空间内必须存在一个圣洁、不可动摇的**绝对原点 $\mathbf{0}$**（因为零向量必须存在）。然而，真实的物理世界（比如一条未经过原点的直线、一张拖动的画布）并不存在天然的原点 。这就催生了**仿射几何（Affine Geometry）** 。

### 1. 点（Point）与向量（Vector）的分离

仿射空间是由一个**点集 $\mathcal{A}$** 和一个**向量空间 $V$** 组合而成的系统 。

* **规则一（点减点得向量）：** 两个“点”绝对不能相加（上海 + 北京毫无意义） ，但两个“点”相减会诞生一个具有方向和长度的“向量” ：



$$B - A = \mathbf{v} \in V \implies \vec{AB} = \mathbf{v} \quad $$


* **规则二（点加向量得点）：** 你不能直接对点施加数乘，但你可以让一个已知的“点”顺着某个“向量”位移，从而诞生一个新的“点” ：



$$A + \mathbf{v} = B \quad $$



### 2. 仿射组合（Affine Combination）与核心约束

既然点不能随便相加，那我们如何像线性代数那样对多个点进行“组合” ？
定义 $P = \sum_{i=1}^n \alpha_i A_i$ 为仿射组合 。要想让这个组合在仿射几何中合法，**其系数必须满足限制： $\sum_{i=1}^n \alpha_i = 1$** 。

#### 为什么系数和必须为 1？（代数本质推导）

我们在仿射空间里任意挑选一个临时参考点 $O$ （注意：这只是一个临时参考点，不是绝对原点 ）。我们将上述组合式改写为相对于 $O$ 点的向量位移形式 ：


$$P - O = \sum_{i=1}^n \alpha_i (A_i - O) = \sum_{i=1}^n \alpha_i A_i - \left(\sum_{i=1}^n \alpha_i\right) O \quad $$

为了让计算结果 $P$ **完全独立于你随机挑选的参考点 $O$** （即无论你把 $O$ 选在哪个位置，最终算出来的物理点 $P$ 不发生偏移），右侧含有 $O$ 的项前面的总系数必须变成 1，从而相互消解 ：


$$\sum_{i=1}^n \alpha_i = 1 \quad \implies \quad P = O + \sum_{i=1}^n \alpha_i \vec{OA_i} \quad$$

这就保证了仿射组合的内在合法性 。若系数和为 0，则该组合的结果会退化为一个**向量** 。

---

### 3. 仿射变换与齐次坐标矩阵表示

仿射变换 $F: \mathcal{A} \to \mathcal{A}$ 是把点映到点的映射 。它的表现形式为一个线性变换加上一个平移向量 ：


$$F(x) = Ax + b \quad $$

由于带有平移项 $b$ ，它**不再具有传统线性映射的齐次性**（因为 $F(0) = b \neq 0$） 。这导致我们无法用一个简单的矩阵乘法来连续表示多次仿射变换（多次变换嵌套会极其复杂） 。

#### 齐次坐标（Homogeneous Coordinates）的矩阵统一

为了将非线性的平移算子强行纳入纯矩阵乘法的世界，工业界（如 GPU 硬件底层底层架构）强行升维，引入了齐次坐标 ：
将 $n$ 维的点 $x$ 尾部强行垫一个 1，写成 $\begin{bmatrix} x \\ 1 \end{bmatrix}$ 。此时，非线性的仿射变换被完美统一成一个 **$n+1$ 阶的线性矩阵乘法** ：


$$\begin{bmatrix} F(x) \\ 1 \end{bmatrix} = \begin{bmatrix} A & b \\ \mathbf{0}^T & 1 \end{bmatrix} \begin{bmatrix} x \\ 1 \end{bmatrix} \quad $$

通过这种升维构造，多次仿射变换的叠加直接简化成了高阶齐次矩阵的连乘 。这就是 GPU 内部进行三维图形渲染时全面采用**四阶矩阵**的代数原因 。

---

### 4. 三角形重心坐标（Barycentric Coordinates）与齐次行列式

在二维仿射平面上，任意三个不共线的点 $\{A, B, C\}$ 构成一个基底三角形 。平面内任意一点 $P$ 都可以唯一地表示为这三个顶点的仿射组合 ：


$$P = \alpha A + \beta B + \gamma C, \quad \text{其中 } \alpha + \beta + \gamma = 1 \quad $$

这一组系数 $(\alpha, \beta, \gamma)$ 被称为点 $P$ 关于该三角形的**重心坐标** 。

#### 重心坐标的几何本质：面积比

利用克莱姆法则（Cramer's Rule）求解上述齐次线性方程组，我们会发现，重心坐标的本质就是**子三角形与大三角形的有向面积之比** ：


$$\alpha = \frac{S_{\triangle PBC}}{S_{\triangle ABC}}, \quad \beta = \frac{S_{\triangle PCA}}{S_{\triangle ABC}}, \quad \gamma = \frac{S_{\triangle PAB}}{S_{\triangle ABC}} \quad $$

而三角形的有效面积（带方向的面积），在齐次坐标下可以用极其漂亮的**三阶行列式**直接计算 ：


$$S_{\triangle ABC} = \frac{1}{2} \det \begin{bmatrix} x_A & x_B & x_C \\ y_A & y_B & y_C \\ 1 & 1 & 1 \end{bmatrix} \quad $$

#### 仿射变换的不变量：面积比恒定

由于仿射变换的矩阵行列式 $\det(A)$ 代表了空间变换前后的面积放大因子 。当对整个空间施加仿射变换时，新旧图形的面积同时乘以 $\det(A)$ ：


$$\frac{\text{新面积 } S_{\tilde{\triangle PBC}}}{\text{新面积 } S_{\tilde{\triangle ABC}}} = \frac{\det(A) \cdot S_{\triangle PBC}}{\det(A) \cdot S_{\triangle ABC}} = \frac{S_{\triangle PBC}}{ S_{\triangle ABC}} = \alpha$$

**核心不变量：** 仿射变换会改变图形的绝对面积、角度和长度 ，但它**绝对不改变任何点关于图形的重心坐标（即面积比例保持永恒）** 。

> 
> 老师的降维打击解题法（“化任意为特殊”） ：
> “几何竞赛中经常会遇到任意三角形内部复杂的面积切割比例问题 。学过仿射几何的同学知道，任意三角形都可以通过一个唯一确定的仿射变换无缝变成一个**等腰直角三角形**或**正三角形** 。既然仿射变换不改变任何面积比例 ，我们直接在最简单的直角三角形里把面积算出来 ，那个答案就必定是任意三角形的通用规律 ！”
> 
> 

#### 现代工业界应用：色彩平滑插值（三角形渲染）

在游戏引擎开发中，已知三角形三个顶点 $A, B, C$ 拥有不同的颜色（如红、绿、蓝） 。如何让三角形内部的所有像素展现出平滑的色彩渐变 ？

1. 计算内部任意像素点 $P$ 的有向面积行列式，解出其唯一的重心坐标 $(\alpha, \beta, \gamma)$ 。


2. 直接利用该坐标对颜色空间进行仿射线性插值 ：



$$\text{Color}(P) = \alpha \cdot \text{Color}(A) + \beta \cdot \text{Color}(B) + \gamma \cdot \text{Color}(C) \quad $$



---

## 第五部分：高维几何架构探秘：超平面、凸集与凸函数

### 1. 超平面（Hyperplane）的代数与几何

在 $\mathbb{R}^n$ 高维空间中，**超平面**是维度为 $n-1$ 的仿射子空间（是将全空间一分为二的绝对屏障） 。

* **一般解析式：** 给定一个固定的非零法向量 $\mathbf{w} \in \mathbb{R}^n$ 和一个常数 $\alpha \in \mathbb{R}$ ，超平面定义为：



$$H = \{x \in \mathbb{R}^n \mid \mathbf{w}^T x = \alpha\} \quad $$

* **几何直观：** 超平面上的所有点 $x$ ，在给定的法向量 $\mathbf{w}$ 方向上的内积都等于常数 $\alpha$ 。超平面将整个高维空间强行切割为两个闭合的**半空间（Half-spaces）** ： $\mathbf{w}^T x \ge \alpha$ 和 $\mathbf{w}^T x \le \alpha$ 。在机器学习（如 SVM 支持向量机）或多面体优化中，我们正是利用超平面作为决策边界，通过代入点计算 $\mathbf{w}^Tx - \alpha$ 的正负号来判断目标落在空间的哪一侧 。



---

### 2. 凸集（Convex Set）与凸组合

* **凸集定义：** 集合 $C$ 内的任意两点 $x, y \in C$ ，它们之间的**整条连线段**也必须全盘包裹在 $C$ 内部 。即：



$$\forall x, y \in C, \quad \theta x + (1-\theta)y \in C, \quad \forall \theta \in [0, 1] \quad $$


* **凸组合（Convex Combination）：** 仿射组合只要求系数和为 1（直线无限延伸） ；而**凸组合**在此基础上施加了**非负约束 $\theta \in [0,1]$** ，使其搜索域内缩锁定在两点之间的有限线段内 。


* **凸多面体（Convex Polytope）：** 若干个闭半空间（即由超平面切出来的半平面）交起来组成的区域，必然是一个凸集 。



---

### 3. 凸函数（Convex Function）的国际标准定义与上方图

在国际学术界，凸函数的定义与部分中学旧教材的叫法（常混淆上凸下凸）有着严格的区别 。

* **严格代数定义：** 函数定义域为凸集 。对函数图像上任意两点进行连线，**连线段（弦）必须始终位于函数图像的上方** ：



$$f(\theta x + (1-\theta)y) \le \theta f(x) + (1-\theta)f(y), \quad \forall \theta \in [0, 1] \quad $$


* **上方图（Epigraph）视界：** 一个函数是凸函数，当且仅当其**上方图（函数曲线上方所有的点构成的点集集合）是一个完美的凸集** ：



$$\text{epi}(f) = \{(x, t) \mid x \in \text{dom}(f), t \ge f(x)\} \quad $$



> 
> 老师的期末划重点语录 ：
> “为什么我们在优化算法里拼了命地寻找凸函数 ？因为凸函数拥有极其伟大的核心性质：它的局部极小值就是全盘的绝对全局最小值 。在凸函数的世界里，只要顺着梯度往下走，只要能算出一个极小值点，你就征服了全空间 。”
> 
> 

---

### 4. 空间分离定理（Hyperplane Separation Theorem）

作为本节课几何代数大融合的终极谢幕，老师给出了一个极其深刻且具有普适性的几何学基本定理 ：
若在 $\mathbb{R}^n$ 空间中存在**两个互不相交的非空凸集 $A$ 和 $B$** ，那么在这个高维空间中，必然可以硬生生地插进一个超平面 $H$ ，完美地将这两个凸集隔绝在超平面的左右两边 。（代数表现为：存在 $\mathbf{w}, \alpha$ 使得 $\forall x \in A, \mathbf{w}^Tx \le \alpha$ 且 $\forall y \in B, \mathbf{w}^Ty \ge \alpha$ ）。这一结论在现代经济学（博弈论均衡分析）和凸优化理论（对偶定理）中，是作为底层最核心的根基定理而存在的 。
